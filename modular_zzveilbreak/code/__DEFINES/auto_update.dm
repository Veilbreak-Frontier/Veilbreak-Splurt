///obj/docking_port/mobile/emergency/proc/on_shuttle_launch()

    //#ifdef TGS
    //if(tgs.IsConnected())
        // This triggers the "Begin Update" flow in TGS 6
        //var/datum/tgs_version/latest = tgs.GetLatestVersion()
        //if(latest)
        //    tgs.PerformUpdate(latest)
      //      log_game("Shuttle launched: TGS update triggered for version [latest.raw_version]")
    //#endif
